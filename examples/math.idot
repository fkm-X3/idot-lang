fn add(x: i32, y: i32) -> i32 { return x + y; }
fn mul(x: i32, y: i32) -> i32 { return x * y; }
fn div(x: i32, y: i32) -> i32 { return x / y; }

fn main() -> i32 {
    let a = 12;
    let b = 4;
    let result = add(a, b) + mul(a, b) - div(a, b);
    return result;
}
