struct Vec3 {
    x: i32,
    y: i32,
    z: i32,
}

fn dot(a: Vec3, b: Vec3) -> i32 {
    return a.x * b.x + a.y * b.y + a.z * b.z;
}

fn main() -> i32 {
    let v = Vec3{ x = 1, y = 2, z = 3 };
    let w = Vec3{ x = 4, y = 5, z = 6 };
    return dot(v, w);
}
