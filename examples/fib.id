# fibonacci in idot

fn fib(n: int) -> int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    let mut i: int = 0
    while i < 10 {
        print_int(fib(i))
        i = i + 1
    }
}
